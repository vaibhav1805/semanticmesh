# ADR-001: Component Type Classification System

**Status:** Accepted
**Date:** 2026-03-19
**Authors:** semanticmesh project team
**Supersedes:** None
**References:** `docs/COMPONENT_TYPES.md`, `docs/CLI_REFERENCE.md`, `docs/CONFIGURATION.md`

---

## Problem

Infrastructure dependency graphs are only useful if AI agents can ask targeted questions like:

- **"If our database fails, what services depend on it?"**
- **"Which critical components need immediate monitoring?"**
- **"How many message brokers do we have, and where are they used?"**

Without component type information, answering these questions requires either:
1. Feeding agents the entire architecture (expensive, slow)
2. Having agents infer types from component names (unreliable, fragile)

This defeats the core value of semanticmesh: allowing AI agents to efficiently query pre-computed dependency graphs for rapid incident response and architectural analysis.

---

## Solution

Every component in the graph carries a **type classification**. This enables:
- **Targeted queries:** `"SELECT * FROM graph_nodes WHERE type = 'database'"`
- **Confidence-aware decisions:** Agents can filter by confidence threshold for risk assessment
- **Extensibility:** Users can define custom types via seed configuration without modifying code

### Design: 12 Core Types

We define 12 component types based on **Backstage** (Spotify's service catalog) and **Cartography** (Lyft's cloud asset inventory) patterns:

1. `service` — Microservices, APIs, applications
2. `database` — Data storage (relational, NoSQL)
3. `cache` — In-memory caching (Redis, Memcached)
4. `queue` — Task queues (RabbitMQ, SQS, Celery)
5. `message-broker` — Event streams (Kafka, NATS)
6. `load-balancer` — Traffic distribution (HAProxy, ELB)
7. `gateway` — API gateways (Kong, Traefik)
8. `storage` — Object storage (S3, GCS)
9. `container-registry` — Image repositories (ECR, Docker Hub)
10. `config-server` — Configuration management (Consul, etcd)
11. `monitoring` — Metrics and observability (Prometheus, Datadog)
12. `log-aggregator` — Centralized logging (ELK, Splunk)

**Plus:** `unknown` type for ambiguous or unclassifiable components.

This taxonomy is:
- **Broad enough** to cover common infrastructure patterns
- **Deep enough** to enable meaningful filtering
- **Familiar enough** that users recognize their own infrastructure in these categories

---

## Design Decisions

### Decision 1: Why 12 Types (Not More, Not Fewer)?

**Options considered:**
- **Flat/single type** (everything is "component") — Loses query power
- **Huge taxonomy** (50+ types) — Overwhelming, difficult to auto-detect reliably
- **12 core types** — Sweet spot

**Rationale:**
- **Coverage:** 12 types cover ~90% of typical infrastructure components
- **Detectability:** Each type has distinctive naming patterns, making auto-detection reliable
- **Extensibility:** Seed config allows users to add custom types for edge cases without bloating the core
- **Precedent:** Backstage and Cartography use similar taxonomies at scale

---

### Decision 2: Confidence Scores (Instead of Binary Classification)

**Options considered:**
- **Binary** (classified or unknown) — Simple, but loses important signal
- **Confidence scores** (0.4–1.0) — Complex, but enables risk-aware queries
- **Multiple candidate types** — Too complex; requires resolution logic

**Rationale:**
- **Reality:** Type detection is probabilistic, not deterministic. A component named "cache-helper" might be a cache helper (high confidence) or a helper service (medium confidence).
- **Agent needs:** AI agents need to assess detection reliability. A 0.95 confidence match is safer to act on than 0.65.
- **Query filtering:** Agents can adjust confidence thresholds for different risk tolerance: `WHERE confidence >= 0.8` for high-confidence queries.
- **Implementation:** Confidence is cheap to compute (already generated by detection algorithms) and easy to persist.

---

### Decision 3: Seed Config for User Extensibility

**Options considered:**
- **Fixed taxonomy only** — Users adapt to our types (inflexible)
- **Code modification** — Users fork and modify types.go (unmaintainable)
- **Seed config file** — Users provide YAML mappings at index time (flexible, maintained)

**Rationale:**
- **Customization:** Organizations have custom component categories (e.g., "internal-tool", "legacy-system"). Seed config lets them define these without code changes.
- **Overrides:** Some components may be consistently misclassified by auto-detection (e.g., "helper-service" always detected as service, but should be cache). Seed config provides escape hatch.
- **Confidence:** Seed-defined mappings get confidence = 1.0 (explicit user intent), superseding auto-detection.
- **Simplicity:** YAML is user-friendly; no code review needed.

---

### Decision 4: Tags for Fine-Grained Metadata

**Options considered:**
- **Ignore metadata** (type only) — Loses important context
- **Type hierarchy** (service > internal-service > critical-internal-service) — Complex, rigid
- **Tags** (primary type + optional tag array) — Flexible, composable

**Rationale:**
- **Flexibility:** Tags capture metadata that doesn't fit neatly into a hierarchy:
  - Criticality: `critical`, `non-critical`
  - Deployment: `internal-only`, `public-facing`
  - Compliance: `pci-dss`, `hipaa`
  - Lifecycle: `deprecated`, `beta`
- **Composability:** One component can have multiple tags; no need for a deep type tree.
- **Query power:** `--include-tags` lets agents search by tag membership without creating new types.
- **Extensibility:** Users define their own tags (no code changes needed).

---

## Trade-offs

### Flexibility vs. Simplicity

**Trade-off:** More types = more flexibility but also more complexity, detection ambiguity, and user confusion.

**Resolution:** 12 core types + seed config provides the best of both:
- Core types handle 90% of use cases (simplicity)
- Seed config handles edge cases (flexibility)
- Users rarely need more than 2–3 custom types

### Auto-Detection vs. Manual Classification

**Trade-off:** Auto-detection is convenient but imperfect; manual classification is accurate but burdensome.

**Resolution:** Hybrid approach:
- **Default:** Auto-detection from naming patterns (0.65–1.0 confidence)
- **Override:** Seed config for persistent misclassifications (confidence 1.0)
- **Fallback:** `unknown` type for ambiguous cases (conservative)

### Single vs. Multiple Types

**Trade-off:** Allowing one component to have multiple primary types increases expressiveness but complicates queries.

**Resolution:** Primary type (singular) + tags (plural):
- Primary type is the main classification (used for filtering)
- Tags capture secondary metadata (optional, searchable separately)
- Queries filter by primary type; tags are supplementary

---

## Related Decisions

1. **SQLite Schema (ADR-002 future):** `graph_nodes.component_type TEXT NOT NULL DEFAULT 'unknown'` persists type classification
2. **Detection Pipeline:** Longest-match strategy for type inference; 3-tier confidence model
3. **Seed Config Format (ADR-003 future):** YAML format for user extensibility; glob patterns for flexible matching
4. **CLI Design:** `semanticmesh list --type TYPE` and `--include-tags` enable type-filtered queries

---

## Consequences

### Positive

- **Capability:** Agents can now answer "what depends on databases?" efficiently
- **Safety:** Confidence scores let agents make risk-aware decisions
- **Extensibility:** Seed config lets users customize without code changes
- **Simplicity:** 12 types are learnable; users understand their own infrastructure

### Negative

- **Complexity:** Detection pipeline is more complex than simple heuristics
- **Maintenance:** Type taxonomy may need updates as infrastructure patterns evolve
- **Ambiguity:** Some components will remain `unknown` or misclassified (trade-off accepted)

### Neutral

- **Database:** Requires `component_type` column on `graph_nodes` (minimal schema impact)
- **API:** Queries now return confidence scores (agents need to handle numeric values)

---

## Implementation Status

- [x] 12-type taxonomy defined (`internal/knowledge/types.go`)
- [x] Detection pipeline implemented (pattern matching + keyword heuristics)
- [x] Confidence scoring integrated (3-tier model)
- [x] SQLite schema extended (`component_type` column)
- [x] CLI command `semanticmesh list --type` implemented
- [x] Seed config framework designed
- [ ] Tag support (planned)
- [ ] User documentation (this document + COMPONENT_TYPES.md, CLI_REFERENCE.md)

---

## References

- **Backstage:** Spotify's service catalog (https://backstage.io/) — inspired component taxonomy
- **Cartography:** Lyft's cloud asset inventory — inspired detection patterns
- **semanticmesh COMPONENT_TYPES.md:** User-facing guide to all 12 types
- **semanticmesh CLI_REFERENCE.md:** Command syntax and query examples
- **semanticmesh CONFIGURATION.md:** Seed config guide for customization

---

## Questions & Discussion

**Q: Why not more types like "database-replica" or "queue-consumer"?**
A: Those are architectural roles, not component categories. We capture them via tags (e.g., `type: database, tags: [replica]`).

**Q: What if my custom type needs confidence scoring?**
A: Seed config supports `confidence_override`. Seed-matched components always get confidence 1.0 (explicit user intent).

**Q: Can I query by tag instead of type?**
A: Currently, tags require post-processing (e.g., `jq` filtering). Future versions may add `--include-tags` filtering to queries.

**Q: How do I know which components are `unknown`?**
A: Query: `semanticmesh list --type unknown` or `SELECT * FROM graph_nodes WHERE component_type = 'unknown'`.

---

**Decision Date:** 2026-03-19
**Accepted By:** semanticmesh project team
**Last Updated:** 2026-03-19
